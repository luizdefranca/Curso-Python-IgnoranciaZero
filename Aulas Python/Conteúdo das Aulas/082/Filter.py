#Filter
#--> Recebe uma função que retorna um booleano
#    e um objeto iterável, e gera um novo
#    objeto a partir dos elementos que tiverem
#    dado True com a função
#l = [x for x in l1 if x%2 == 0]
#ou
#l = list(filter(lambda x: x%2 == 0, l1))
